# Examples

This folder contains example configuration and common `zaibflow run` recipes to help new users get started.

Files:

- `config.yaml` — sample ZaibFlow config with a custom provider and an OpenRouter alias.
- `recipes.md` — common `zaibflow run` commands and tips.
